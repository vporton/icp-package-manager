{
  "name": "repository_frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "setup": "npm i && dfx canister create package_manager_backend && dfx generate package_manager_backend && dfx deploy",
    "start": "vite --port 3000",
    "build": "tsc && vite build",
    "format": "prettier --write \"src/**/*.{json,js,jsx,ts,tsx,css,scss}\""
  }
}
